# Application Agent Rules

This prototype is approved for the scoped O1 build. Keep execution trusted-checkout
only, preserve real Cargo evidence, and do not add arbitrary browser-triggered
commands, repository URLs, or compiler flags.
